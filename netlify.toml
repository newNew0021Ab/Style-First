[build]
  base    = "artifacts/museum-quest"
  command = "pnpm install --no-frozen-lockfile && pnpm run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
